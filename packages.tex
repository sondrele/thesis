\usepackage[utf8]{inputenc}
\usepackage[english]{babel}

% For a directory tree
\usepackage{dirtree}

% Used for new commands taking more than one argument
\usepackage{xargs}

% Lorem ipsum
\usepackage{lipsum}

% For b5 papers instead of a4
% The margins is from the example project at https://daim.idi.ntnu.no/faq_innlevering.php
% If we don't explicitly set the margins, the indentation will be different for left/right pages
% We can decide later whether we want margins like this or not
\usepackage[lmargin=25mm,rmargin=25mm,tmargin=27mm,bmargin=30mm]{geometry}
% \geometry{b5paper}

% Multirow and multicolumn tables
\usepackage{multirow}

% Line break in tables
\usepackage{pbox}

% Create and format mathematical things
\usepackage{amsmath}

% For \checkbox and other symbols
\usepackage{amssymb}
\usepackage{tipa}
% Provides frowny face \Frowny
\usepackage{marvosym}

% Rotate text sideways, might be used in tables
\usepackage{rotating}

% Generate urls
\usepackage{url}

% Nesting includes
\usepackage{newclude}

% Correct citations...? Not sure, doesn't hurt
\usepackage{natbib}

% Floating in tables and figures
\usepackage{float}

% Double line shift instead of indentation for paragraphs
\usepackage[parfill]{parskip}

% Automatically include 'Figure' with \autoref
\usepackage{hyperref}
\newcommand*{\Appendixautorefname}{Appendix}
% Has to be included after hyperref, makes numbering in pdf index
\usepackage[numbered]{bookmark}

% Makes the bibliography part of the contents
\usepackage{tocbibind}

% Per-chapter numbering of figures and tables
\usepackage{chngcntr}
% See: https://tex.stackexchange.com/questions/28333/continuous-v-per-chapter-section-numbering-of-figures-tables-and-other-docume
% \counterwithin{figure}{section}
% \counterwithin{table}{section}

% To provide appendices in the contents
\usepackage[toc,page]{appendix}

% \usepackage{pgfplots}
% \usepackage[nottoc]{tocbibind}

% For listings. This package requires the pdf to be compiled with '-shell-escape' and that
% python-pygments is found in the system path
% Documentation: ftp://ftp.dante.de/tex-archive/macros/latex/contrib/minted/minted.pdf
\usepackage{minted}
% Predife style for Rust and C code. Usage: \begin{rustcode} ... \end{rustcode}
\newminted{rust}{autogobble,linenos,frame=leftline}
\newminted{c}{autogobble,linenos,frame=leftline}

\usepackage{caption}
\usepackage{subcaption}
\usepackage{graphicx}

% Defines checkmark and X's used in tables
\usepackage{amssymb}% http://ctan.org/pkg/amssymb
\usepackage{pifont}% http://ctan.org/pkg/pifont
\newcommand{\cmark}{\ding{51}}%
%\newcommand{\xmark}{\ding{55}}%   this is currently not used

% Used for multiple footnote with same number
\usepackage{scrextend}

% Can use \begin{enumerate}[1)]
\usepackage{enumerate}
